# Current Workspace Rules

- **Release Documentation Policy**: For every application release, always create a release notes markdown document in the conversation artifacts directory named after the release version (e.g. `<version>.md` like `1.0.0.1.md`).
